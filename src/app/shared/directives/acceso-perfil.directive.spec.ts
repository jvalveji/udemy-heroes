import { AccesoPerfilDirective } from './acceso-perfil.directive';

describe('AccesoPerfilDirective', () => {
  it('should create an instance', () => {
    const directive = new AccesoPerfilDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});

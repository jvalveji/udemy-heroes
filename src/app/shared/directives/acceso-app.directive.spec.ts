import { AccesoAppDirective } from './acceso-app.directive';

describe('AccesoAppDirective', () => {
  it('should create an instance', () => {
    const directive = new AccesoAppDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});

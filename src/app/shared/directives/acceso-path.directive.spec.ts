import { AccesoPathDirective } from './acceso-path.directive';

describe('AccesoPathDirective', () => {
  it('should create an instance', () => {
    const directive = new AccesoPathDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});

import { AutoRealceDirective } from './auto-realce.directive';

describe('AutoRealceDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoRealceDirective(null, null);
    expect(directive).toBeTruthy();
  });
});

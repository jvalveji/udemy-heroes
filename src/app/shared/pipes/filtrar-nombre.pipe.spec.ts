import { FiltrarNombrePipe } from './filtrar-nombre.pipe';

describe('FiltrarNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarNombrePipe();
    expect(pipe).toBeTruthy();
  });
});

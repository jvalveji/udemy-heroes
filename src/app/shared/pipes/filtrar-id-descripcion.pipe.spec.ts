import { FiltrarIdDescripcionPipe } from './filtrar-id-descripcion.pipe';

describe('FiltrarIdDescripcionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarIdDescripcionPipe();
    expect(pipe).toBeTruthy();
  });
});

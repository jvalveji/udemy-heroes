import { FiltrarDescripcionPipe } from './filtrar-descripcion.pipe';

describe('FiltrarDescripcionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarDescripcionPipe();
    expect(pipe).toBeTruthy();
  });
});
